import PageContainer from "@/components/shared/PageContainer";
import SectionHeader from "@/components/shared/SectionHeader";

export default function BusinessPage() {
  return (
    <PageContainer>
      <SectionHeader
        title="Business"
        subtitle="Manage ideas, projects, and investments."
      />
    </PageContainer>
  );
}