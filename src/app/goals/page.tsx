import PageContainer from "@/components/shared/PageContainer";
import SectionHeader from "@/components/shared/SectionHeader";

export default function GoalsPage() {
  return (
    <PageContainer>
      <SectionHeader
        title="Goals"
        subtitle="Track your progress and stay focused."
      />
    </PageContainer>
  );
}
