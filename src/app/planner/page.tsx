import PageContainer from "@/components/shared/PageContainer";
import SectionHeader from "@/components/shared/SectionHeader";

export default function PlannerPage() {
  return (
    <PageContainer>
      <SectionHeader
        title="Planner"
        subtitle="Organize your day, week, and future."
      />
    </PageContainer>
  );
}
